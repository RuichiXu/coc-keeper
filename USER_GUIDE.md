# CoC Keeper 用户手册

> 适用于 2026-09-05 合并后的前端。DSH 插件与独立网页版使用同一套面板；安装与启动见 [README](README.md) 和 [独立网页版说明](standalone/README.md)。

快速跳转：[第一次开团](#2-第一次开团) · [日常主持](#3-主持与日常操作) · [剧情网络](#4-阅读剧情网络) · [质量与校对](#5-质量报告与校对) · [面板管理](#7-玩家视图切换游戏与面板管理) · [常见问题](#8-常见问题)

## 1. 先认识面板

页面中有「🎲 CoC Keeper」和「🎭 玩家视图」两个浮动面板。Keeper 面板用于主持和管理场次，玩家视图用于查看已公开信息、输入行动。

| Keeper 顶部入口 | 去这里做什么 |
|---|---|
| **主持** | 聊天推进剧情、快捷明骰/暗骰、处理检定、预览并执行 KP 指令 |
| **剧情** | 在「状态总览」管理场景、时间、人物状态、物品和任务；在「剧情结构」跟进关键点、分支和提醒 |
| **解析** | 阅读剧情网络，搜索关键点与结局，查看条件、质量报告，校对解析草稿 |
| **调试** | 导入、实体、人物、卡库、运行、契约、设置 |

旧版的聊天、状态、导入等功能已经归入上述入口。切到剧情、解析或调试后，聊天区会让出空间；回到主持即可继续对话。

## 2. 第一次开团

### 准备模型和资料

1. 打开 **调试 → 设置**，检查 LLM 配置，按界面保存并测试连接。已有可用配置时可直接继续。
2. 打开 **调试 → 导入**，上传剧本、规则或人物卡，也可以粘贴文本；按资料选择类型，或使用自动识别。
3. 等待导入进度完成，检查结果。剧本的结构与深度解析需要时间；导入完成后可在已导入内容中分段阅读全文。
4. 到 **调试 → 卡库** 检查剧本资产和调查员卡。卡库用于跨场次复用资料，当前场次的人物状态在 **剧情 → 状态总览** 或 **调试 → 人物** 查看。

PDF 需有可提取文字，扫描件暂不支持 OCR；DOC 提取效果不稳定时可改用 DOCX、TXT 或粘贴文本。导入前确认标题栏中的当前场次，避免把资料导进其他游戏。

### 新建场次向导

点击 Keeper 标题栏的 **＋**，依次完成三个准备步骤：

1. **选择剧本**：填写场次名称，选择已入库的剧本。也可以不选剧本，创建空场次。刚完成导入却没看到资料时，点「刷新列表」。
2. **调查员**：为玩家调查员点击「加入」，可选多张；需要时在 AI 调查员下拉框中选择一张 AI 卡。不选调查员继续时会出现空场次确认。
3. **确认**：核对场次、剧本和人物，点击「创建并开始」。

创建成功后会显示开场白，点击 **进入场次 →** 切到新游戏。成功页在三步之后，不是第四个需要填写的步骤。向导可用「上一步」返回，也可点「关闭」或按 Escape 退出。

卡库中的剧本「开新场次」会打开同一向导并预选剧本。调查员卡或实体资产的「加入场次」则加入当前选中的场次。

## 3. 主持与日常操作

**对话**：在主持页输入行动，按 Enter 或点击「发送」；Shift+Enter 换行。查看历史记录时，新消息不会强制把你拉回底部；回到底部后继续跟随新消息。

**快捷骰**：填写骰式（默认 `d100`）、目标技能值、常规/困难/极限难度和检定说明，再选择明骰或暗骰。明骰结果公开，暗骰用于 KP 的秘密判定。已有待处理检定时，优先使用检定区对应操作，以便结果与当前检定关联。

**检定与 KP 指令**：宽面板中位于对话右侧，窄面板中排列到下方。自然语言 KP 指令先点「解析预览」，检查将要执行的操作，再「确认执行」。普通玩家行动直接从聊天输入即可。

**状态总览**：在剧情页切到「状态总览」，查看和编辑概述、场景、游戏内时间、KP 模式、人物数值、物品与任务。此处的按钮会修改当前场次状态。

**剧情结构**：查看关键剧情点、分支和提醒；通过对应操作揭示关键点、标记抵达、选择分支选项或登记提醒。它面向当前游戏进展；准备剧本、核对全局线路时使用解析页。

## 4. 阅读剧情网络

进入 **解析**，默认打开「骨架总览」。没有深度解析时会显示提示，可从「前往导入」准备剧本。

### 两种总览

| 视图 | 适合阅读的内容 |
|---|---|
| **骨架总览** | 从左向右看主线、分支、并行调查、汇聚与结局；检定细节收敛到节点索引 |
| **场景总览** | 以场景聚合节点阅读场景之间的关系；点场景可继续查看其成员关键点 |

金色线路用于识别主线，圆形表示关键点，场景聚合使用矩形，菱形表示分支，星形表示结局；带「自」的分支表示自动判定。`↩` 表示可返回，不必在图中反复画出返回节点。

悬停在节点或边上，会突出关联线路。点击节点查看条件、说明与关联信息；点击连线查看连接详情。场景聚合边的详情可继续打开原始连接。更完整的说明在 **检查栏 → 显示与图例**，其中还可以勾选「显示返回边」。

图上节点/边数量会随视图聚合、隐藏返回边等操作改变。线路图用于理解结构，某个结局显示在图上并不代表当前场次已经满足它的全部前置条件。

### 在大图中移动

- **滚轮**：以鼠标位置为中心缩放；也可以用「＋」「−」。
- **拖拽空白画布**：平移。
- **适应画布**：把整张图缩进当前可用空间；长剧本缩得很小时会暂时隐藏节点文字，放大后恢复。
- **100%**：恢复原始比例和起始位置。
- **图下方迷你导航**：点击跳到对应位置；聚焦后可用左右方向键移动。
- **检查栏 / 收起检查栏**：展开详情区域，或给图留出更多空间。窄面板中检查栏覆盖在图上，可随时收起。

### 搜索节点与寻找结局

顶部输入框支持标题、ID 或场景名，旁边可以按节点类型和场景筛选。匹配节点突出显示，其他节点淡化以保留线路上下文；检查栏列出匹配结果。

点击结果可查看详情，并在有对应图形时定位。被折叠的检定关键点/分支仍可从索引找到，即使它没有独立显示在当前图上。

没有筛选时，检查栏默认显示 **结局导航**。寻找所有结局时点击「清除筛选」，再逐个打开；不要只凭当前屏幕可见的星形数量判断结局是否缺失。

## 5. 质量报告与校对

打开 **解析 → 检查栏 → 质量统计与审校报告**，可以看到解析来源、草稿/已确认状态，以及保存的结构、规则、审校记录和分块记录计数。展开问题列表后可按严重程度筛选，阅读问题与建议。

**0 条问题记录不等于执行过语义审校。** 当前后端在确定性检查没有高/中问题时可能跳过语义审校；已有报告未完整标记执行情况。没有报告会显示「暂无质量报告」。「已确认生效」表示采用了保存的解析版本，也不表示自动证明剧情正确。

需要手动改节点、边、结局或条件时：

1. 展开 **校对编辑与确认生效**，先保留一份原 JSON 备份。
2. 在 JSON 编辑框中修改内容，点击 **保存草稿**，确认保存成功。
3. 刷新后的内容核对无误，再点 **确认生效**。

确认按钮只作用于已经保存的版本，**不会自动保存输入框中的改动**。当前提供的是 JSON 校对，没有通过拖动节点来修改剧情连接的图形编辑器；拖拽画布只改变观看位置。

章节、场景及结构层级在 **剧本结构编辑** 中单独修改并「保存结构编辑」。这会改变剧本结构，应先核对字段和当前场次。缺少结构数据时，按页面提示重新导入或解析。

## 6. 调试页的其他入口

| 子入口 | 用法 |
|---|---|
| **导入** | 文件/文本导入；查看导入进度与已导入全文 |
| **实体** | 管理 NPC、地点、物品等，编辑状态，揭示或隐藏；揭示时填写适合玩家看到的内容 |
| **人物** | 查看当前场次的人物属性、物品，更新 HP/SAN/MP/LUCK；新增人物可用导入、卡库或向导 |
| **卡库** | 管理可复用的剧本、调查员卡和实体资产；从资产创建场次或加入当前场次 |
| **运行** | 查看运行诊断、检定、事件与门禁信息；剧情点跳转和夹具导出用于复现问题 |
| **契约** | 查看、编辑契约草稿并确认生效，供运行时校验使用 |
| **设置** | 保存和测试 LLM 配置，管理内置规则导入 |

运行页的「剧情点跳转（测试）」会重设剧情状态，应在测试场次中使用。删除剧本资产时，界面会提示**引用它的场次也会一并删除**；只想结束一个游戏时使用标题栏更多菜单中的「删除当前场次」。

## 7. 玩家视图、切换游戏与面板管理

玩家视图显示服务端提供的玩家可见场景、人物、已公开实体/线索与最近动态，并可发送行动。Keeper 与玩家面板的场次选择器共用同一个当前游戏；在任一处切换都会影响另一个面板。刷新浏览器后会记住当前场次。

玩家视图是公开信息的展示面板；同一页面还可以打开 Keeper 面板。给玩家共享屏幕时应选取玩家视图，避免把 Keeper 的解析、暗骰和未揭示内容一并展示。

Keeper 标题栏操作：

| 控件 | 作用 |
|---|---|
| 场次下拉框 | 切换当前游戏 |
| **＋** | 新建场次向导 |
| **⟳** | 刷新当前状态 |
| **⛶** | 最大化/还原 |
| **···** | 重置位置与大小；删除当前场次 |
| **−** | 最小化到面板坞 |

按住标题栏可拖动面板，边缘/右下角可调整 Keeper 大小，位置与尺寸会自动记忆。右下角 **🧩 面板坞** 列出可用面板，点击条目可打开或隐藏；最小化后的 Keeper 也从这里恢复。

玩家视图标题栏的收起按钮折叠内容和输入区，再点可展开；面板坞控制整个玩家面板的显隐。Keeper 约 2.5 秒、玩家视图约 3 秒轮询，其他入口的操作可能稍后才显示，可等待下一次更新或刷新页面。

## 8. 常见问题

| 现象 | 排查方式 |
|---|---|
| 找不到 Keeper | 先从右下角面板坞恢复；仍未挂载时核对插件是否启用，重启服务后刷新浏览器 |
| 面板尺寸不合适或位置偏移 | 更多菜单 → 重置位置与大小；需要看大图时最大化并收起检查栏 |
| 向导没有剧本或人物卡 | 先到调试 → 导入，再在卡库确认资产；向导中刷新列表 |
| 解析页没有网络图 | 核对当前场次与导入结果；已导入文本不一定已有可用深度解析 |
| 图上文字消失 | 当前缩放可能小于 30%，放大或搜索目标节点 |
| 搜索不到想看的结局 | 清除关键词、类型与场景筛选，再看检查栏的结局导航 |
| 校对改动没有生效 | 确认 JSON 合法，并按“保存草稿 → 核对 → 确认生效”操作 |
| 聊天/导入失败 | 查看界面错误，在调试 → 设置检查配置并测试连接；失败信息与问题场次一起提供给维护者 |

反馈 UI 问题时，请注明使用 DSH 还是独立网页版、浏览器、场次、当前 Tab/网络视图，以及复现步骤。截图可包含面板尺寸、缩放比例和错误提示；分享前移除密钥、访问令牌及不适合公开的剧本内容。
